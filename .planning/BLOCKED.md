# Отложенные задачи

Пишет `node tools/blocked.mjs`. Задача застряла: три честные попытки не прошли,
и без человека не решить, виноват код, гейт или сама задача. Очередь идёт дальше,
а разбираться с этим списком нужно отдельно — он не рассосётся сам.

## R27 — отложена 2026-08-23 21:42
Фигура не читается одним предметом: на обоих кадрах (разные сиды) человек видит только скопление символов, а не цельную невозможную фигуру. На втором кадре крупный засвет. Гейты figure-check и impossible-check проходят (швы сходятся в проекции, расходятся в пространстве), но визуально фигура теряется в поле глифов. Проблема в src/world/world.js или src/atmosphere/impossible.js — нужно менять плотность/контраст/размер фигуры относительно поля.
## N31 — отложена 2026-08-23 22:34
пол на y=-120, камера на y=0, при FOV 70 пол вне кадра, world-check требует чтобы все облака были видны, но пол и надпись вне кадра, не могу менять floorY (контракт говорит bounds.min[1]) и не могу менять world-check.mjs (пороги менять запрещено)
## N31 — РАЗОБРАНО, возвращена в очередь
Проверено замером, а не по отчёту. `world-check` падал на шести облаках, и случаев
там было два разных.
**Гейт был неправ насчёт пяти.** Облака 6–10 — это отражения пола. Отражение лежит
НИЖЕ линии пола, и от камеры, глядящей ровно вдаль, оно законно вне кадра: его видно,
когда смотришь на пол. Правило «каждое облако видно от точки входа» писалось до того,
как в проекте появился пол, и для отражения оно неверно. Правило уточнено: облака с
`userData.floorPart === "mirror"` из него исключены, и в коде гейта записано, что их
продолжает сторожить `floor-check` (зеркальность, плотность, затухание, проводка fade).
Пробовал вместо исключения мерить отражение камерой, наклонённой к полу. Не вышло:
камерой каждый кадр управляет `flycam` и возвращает её обратно до отрисовки, замер
давал нули и абсурдные числа — 64989 «своих» пикселей у облака из 30 точек. Врущий
замер в гейт не идёт, поэтому исключение, а не подделка проверки.
**Гейт прав насчёт одного.** Облако 12 — надпись, 1500 точек, ноль пикселей. Это
настоящий дефект: надпись обязана читаться с расстояния (признак 12 референса).
Плоскость пола при этом видна честно — 349 пикселей, так что дело не в поле.
Что осталось сделать — в тексте N31.
## N31 — отложена 2026-08-23 23:49
предстартовая проверка провалилась ДО начала задачи: облако 12 (1500 точек) даёт 0 пикселей — надпись не видна. Это поломка, которую принесла предыдущая задача, а не N31. next-task велел не начинать новую задачу. Прошлый круг по gl_PointCoord/uv не повторять: вместо догадок нужен один бисекционный опыт (заменить выборку атласа на сплошной цвет), но делать его допустимо только после того, как человек решит: откатить последнюю закрытую задачу (undo-task) или разрешить чинить поломку внутри сессии N31.
## N31 — РАЗОБРАНО (2), возвращена в очередь
Диагноз «поломку принесла предыдущая закрытая задача» неверен, откат не нужен.
Проверено по истории и по рабочему дереву, а не по отчёту.
`git show --stat` последней закрытой N30: в коммите ровно два файла — BACKLOG.md и
новый `src/world/textField.js`. Ни `boot.js`, ни `world.js` она не трогала, надпись
в сцену не подключала. Откат N30 удалил бы модуль, который сейчас импортирует boot.js,
и не тронул бы ни одной строки, создающей облако 12.
Облако 12 создаётся в НЕЗАКОММИЧЕННОМ `src/boot.js` — в работе самой N31 (`git status`
показывает `M src/boot.js`, в диффе `buildFormulaPlane(... count: 1500 ...)` и
`scene.add(formulaField)`). Оттуда же берутся облака 6-10 (отражение пола).
То есть ноль пикселей у надписи — дефект незаконченной проводки N31, а не регрессия.
`undo-task` в таком состоянии всё равно бы отказал: он не работает на грязном дереве.
Что изменено в конвейере, чтобы это не повторилось: `next-task` перед фразой про
«поломку из прошлого» смотрит `git status` и, если дерево грязное, показывает список
незакоммиченных правок и прямо запрещает откат; правило 5 в START.md — то же самое
словами. Задача возвращена в todo: доделывать проводку, а не искать виноватого.
