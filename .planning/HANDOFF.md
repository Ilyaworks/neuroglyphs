# HANDOFF — читать первым в новой сессии

> Порядок старта: **1)** прочитать этот файл целиком, **2)** запустить
> `node tools/project-status.mjs`, **3)** только потом что-то менять.
> Ниже — требования заказчика дословно и их проверенный статус. Не считать
> выполненным ничего, что не подтверждено командой или замером.

## Требования заказчика (дословно, две реплики)

1. «удалить все от мертвой концепции, добавить формы, но только те, где высокая
   плотность частиц, что за тест детерменизма я не знаю, также нужно сделать так, чтобы
   в проекте информация и в задаче обновлялась так, чтобы ты клод понимал точно, что
   происходит и имел полное представление по проекту. Также мне нужен будет обновленный
   список форм.»
2. «Должны быть подготовлены те, что со сферой в середине и кольцом и те, которые в разных
   формах. Те что с разными формами не обязательно должны иметь сферу по центру.» +
   «Главное, чтобы не было откровенных дублей.» +
   «Можешь брать в качестве референса также галактики из космоса и другие космические
   объекты или даже туманности.» +
   «Все равно везде по центру скопление… сделай все же нормальные не только с центром.»

## Статус по каждому пункту (проверено)

| Требование | Статус | Чем подтверждено |
|---|---|---|
| Убрать мёртвую концепцию v1 из кода | **сделано** | в `main.js` нет `tokens`/`CONTEXT_MAX`; в HUD вместо них SEED и SHAPE; `main.js` 2072 → ~480 строк |
| Формы только плотные | **сделано** | отбор в `tools/pick-dense-shapes.mjs`: объём ≥ 0.12, пик ≤ 0.45; 210 форм → 92 в мире |
| Старые формы не терять | **сделано** | все 169 сохранены в `src/world/legacyShapes.js`, объединены в `allShapes.js` |
| Две группы: «сфера + кольцо» и «разные» | **сделано частично** | `CORE_RING_SHAPES` (26) и `VARIED_SHAPES` (66) в `src/world/fieldShapes.js`; см. дыру ниже |
| Без откровенных дублей | **сделано** | дедупликация по отпечаткам внутри каждой группы; из 88 вариаций тора в мире осталось 6 |
| Обновляемая картина проекта | **сделано** | `node tools/project-status.mjs`; правило старта и финала сессии — в `.clinerules` |
| Обновлённый список форм | **сделано** | `seeds.html` (по сиду на каждую форму) + `src/world/preview.html` |
| «Не только с центром» | **сделано сегодня, требует проверки глазами** | светило и кольцо теперь зависят от хеша сида: половина миров без них, в консоли `core=on/off` |
| Настоящие «светило + кольцо с провалом» | **НЕ СДЕЛАНО** | см. ниже |

## Главная незакрытая дыра

Заказчик хочет композицию «сфера в середине, вокруг кольцо, между ними пусто».
Я измерил провал плотности (во сколько раз ядро и кольцо плотнее промежутка между ними)
по всем 210 формам:

```
mushroom          2.26   <- единственная настоящая
stepped           1.01
accretionHalo     0.88
globularBloom     0.82
ringedStar        0.77
stellarCorona     0.77
```

Вывод: **таких форм в наборе практически нет.** Четыре «звёздные» формы, сделанные
локальной моделью, — это облака с тяжёлым центром без пустоты вокруг ядра. То, что
заказчик видел раньше, давали два независимых объекта сцены: `buildNeuralCore` (шар
глифов в центре) и `buildContextRing` (кольцо, вращается в обратную сторону). Их удалять
нельзя, но с сегодняшнего дня они не в каждом мире.

**Задача на следующую сессию:** создать формы-«светила с кольцом» по-настоящему, с
провалом ≥ 1.3, и формы с честно пустой серединой, чтобы миры не были однообразными.
Референсы от заказчика: галактики, туманности, космические объекты — кольцевая туманность,
спиральная галактика с балджем и провалом перед рукавами, аккреционный диск вокруг чёрной
дыры, кольца Сатурна со щелью Кассини, планетарная туманность, шаровое скопление.

Метрика уже встроена: колонка «провал» в `node tools/shape-check.mjs`.

## Не закрыто из первой реплики заказчика

1. **Тест детерминизма не починен.** `test/determinism.test.mjs` не импортирует production-код:
   внутри лежит копия старой раскладки, которой в проекте нет. Он всегда зелёный, даже если
   мир перестанет быть детерминированным. Надо переписать так, чтобы он проверял реальную
   цепочку: `decodeSeed` -> выбор формы из `fieldShapes.js` -> вызов формы из `allShapes.js`,
   и сравнивал побайтово два прогона одного сида, а также различие двух разных сидов.
   Формы, использующие общий генератор (старые), требуют `setRng` — учесть это в тесте.
2. **Косметика мёртвой концепции.** Функция `buildContextRing` в `main.js` называется по
   старой концепции (переименовать в `buildOrbitRing` вместе с вызовом и переменной `ring`
   при желании) и в `index.html` остались неиспользуемые CSS-правила `#context-bar`
   и `#context-fill` — блоки разметки удалены, стили нет.

## Найденная причина низкой заполненности форм (проверено замером)

Хеш `h(n)`, который я вписал в промт S1 и который используют все новые формы в
`shapePatch.js`, **вырожден**: соседние аргументы дают связанные значения.

```
                     корреляция h(i), h(i+1)   заполненность xyz из чистого хеша
текущий h(n)                 -0.4184                        0.085
splitmix32                   -0.0008                        0.771
Math.random (эталон)            —                           0.767
```

Из-за этого точки слипаются, и все формы, построенные на `h(i)`, получают низкий объём —
отсюда и «везде скопление», и провал у четырёх «звёздных» форм. **Первым делом в новой
сессии надо заменить хеш**, а потом уже делать новые формы: старые претензии могут уйти
сами. Проверенная замена:

```js
export function h(n) {
  let x = (n + 0x9e3779b9) >>> 0;
  x ^= x >>> 16; x = Math.imul(x, 0x21f0aaad) >>> 0;
  x ^= x >>> 15; x = Math.imul(x, 0x735a2d97) >>> 0;
  x ^= x >>> 15;
  return (x >>> 0) / 4294967296;
}
```

После замены обязательно: `node tools/pick-dense-shapes.mjs`,
`node tools/gen-seeds-gallery.mjs`, `node tools/shape-check.mjs` — геометрия всех форм
на `h(i)` изменится, списки и галерею надо пересобрать.

## Особенность OpenChamber, из-за которой терялось время

Приложение читает `~/.config/opencode/opencode.json` **при запуске**. Если поменять
`baseURL` (например, переключить на прослойку) при открытом приложении, работающие сессии
продолжат ходить по старому адресу — и модель снова уйдёт в размышления на десятки тысяч
символов. После правки конфига OpenChamber надо закрыть полностью и открыть заново.

## Как устроены формы (важно для правок)

- `src/world/legacyShapes.js` — 169 старых форм, перенесены из `main.js`. Используют общий
  сеяный генератор, `main.js` подставляет его через `setRng()`.
- `src/world/shapeCatalog.js` — 40 новых форм; `shapePatch.js` подменяет одноимённые и
  добавляет новые (механизм сделан специально, чтобы **не править большой файл**).
- `src/world/allShapes.js` — объединение: при совпадении имён побеждает каталог.
- `src/world/fieldShapes.js` — **генерируется**, руками не править:
  `node tools/pick-dense-shapes.mjs`, затем `node tools/gen-seeds-gallery.mjs`.
- Контракт формы: `(i, p, out)` пишет `out[0..2]`; никакого `Math.random()`; параметры
  только из 16 полей `p` (radius, flatten, distPow, tubeR, arms, twist, spread, thickness,
  strands, turns, clusterCount, clusterRadius, freq, amp, knotP, knotQ).

## Ограничения окружения

- Локальная модель (Qwen3.8-27B через OpenChamber/opencode) **надёжно создаёт новые файлы
  и не умеет править большие**. Всё, что касается `main.js`, делает Claude.
- Прослойка `C:\Users\onlin\cline-llm-proxy\start-proxy.cmd` должна быть запущена: без неё
  модель уходит в размышления и молча обрывает ход по лимиту вывода.
- Промты для модели — по-русски, приказом, с одной командой проверки и явным STOP.

## Долги из аудита (не срочные, но реальные)

`test/determinism.test.mjs` не проверяет production-код: внутри копия старой раскладки,
поэтому зелёный результат ничего не значит. Атлас глифов не кэшируется и не освобождается
(утечка при пересеве в T07). 750 тысяч точек строятся синхронно — отсюда пауза на загрузке.
`gl_PointSize` не ограничен сверху. `server.js` слушает все интерфейсы.
